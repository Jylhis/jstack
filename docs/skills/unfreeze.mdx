---
title: "/unfreeze"
sidebarTitle: "/unfreeze"
description: "Remove the /freeze boundary, allowing edits everywhere again."
---

`/unfreeze` removes the [`/freeze`](/skills/freeze) boundary, allowing edits everywhere again.

The hooks stay registered for the session — they just allow everything. Run `/freeze` again to set a new boundary.

<CardGroup cols={2}>
  <Card title="/freeze" icon="lock" href="/skills/freeze">
    Set a new edit boundary.
  </Card>
  <Card title="/guard" icon="shield" href="/skills/guard">
    Full safety mode.
  </Card>
</CardGroup>
