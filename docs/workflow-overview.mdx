---
title: "The Sprint"
sidebarTitle: "The Sprint"
description: "How jstack skills connect into a complete sprint workflow: Think, Plan, Build, Review, Test, Ship, Reflect."
---

jstack is a process, not a collection of tools. The skills run in the order a sprint runs:

**Think → Plan → Build → Review → Test → Ship → Reflect**

Each skill feeds into the next. `/office-hours` writes a design doc that `/plan-ceo-review` reads. `/plan-eng-review` writes a test plan that `/qa` picks up. `/review` catches bugs that `/ship` verifies are fixed. Nothing falls through the cracks because every step knows what came before it.

## The full skill table

| Skill | Your specialist | What they do |
|-------|----------------|--------------|
| [`/office-hours`](/skills/office-hours) | **YC Office Hours** | Start here. Six forcing questions that reframe your product before you write code. Design doc feeds into every downstream skill. |
| [`/plan-ceo-review`](/skills/plan-ceo-review) | **CEO / Founder** | Rethink the problem. Find the 10-star product. Four modes: Expansion, Selective Expansion, Hold Scope, Reduction. |
| [`/plan-eng-review`](/skills/plan-eng-review) | **Eng Manager** | Lock architecture, data flow, diagrams, edge cases, and tests. |
| [`/plan-design-review`](/skills/plan-design-review) | **Senior Designer** | Rates each dimension 0-10, explains what a 10 looks like, then edits the plan. AI Slop detection. |
| [`/design-consultation`](/skills/design-consultation) | **Design Partner** | Build a complete design system from scratch. Researches the landscape, proposes creative risks, generates mockups. |
| [`/review`](/skills/review) | **Staff Engineer** | Find the bugs that pass CI but blow up in production. Auto-fixes the obvious ones. |
| [`/investigate`](/skills/investigate) | **Debugger** | Systematic root-cause debugging. Iron Law: no fixes without investigation. |
| [`/design-review`](/skills/design-review) | **Designer Who Codes** | Live-site visual audit + fix loop. 80-item audit, atomic commits, before/after screenshots. |
| [`/qa`](/skills/qa) | **QA Lead** | Test your app, find bugs, fix them, re-verify. Auto-generates regression tests. |
| [`/qa-only`](/skills/qa-only) | **QA Reporter** | Same methodology as /qa but report only. Pure bug report. |
| [`/cso`](/skills/cso) | **Chief Security Officer** | OWASP Top 10 + STRIDE threat model. Zero-noise, 8/10+ confidence gate. |
| [`/ship`](/skills/ship) | **Release Engineer** | Sync main, run tests, audit coverage, push, open PR. |
| [`/land-and-deploy`](/skills/land-and-deploy) | **Release Engineer** | Merge PR, wait for CI and deploy, verify production health. |
| [`/canary`](/skills/canary) | **SRE** | Post-deploy monitoring loop. Watches for errors and performance regressions. |
| [`/benchmark`](/skills/benchmark) | **Performance Engineer** | Baseline page load times, Core Web Vitals, resource sizes. Compare before/after. |
| [`/document-release`](/skills/document-release) | **Technical Writer** | Update all project docs to match what you just shipped. |
| [`/retro`](/skills/retro) | **Eng Manager** | Team-aware weekly retro. Per-person breakdowns, shipping streaks, growth opportunities. |
| [`/browse`](/browser/overview) | **QA Engineer** | Real Chromium browser, real clicks, real screenshots. ~100ms per command. |
| [`/setup-browser-cookies`](/browser/setup-cookies) | **Session Manager** | Import cookies from your real browser into the headless session. |
| [`/autoplan`](/skills/autoplan) | **Review Pipeline** | One command, fully reviewed plan. CEO → design → eng review automatically. |

### Power tools

| Skill | What it does |
|-------|-------------|
| [`/codex`](/skills/codex) | **Second Opinion** — independent review from OpenAI Codex CLI. Cross-model analysis. |
| [`/careful`](/skills/careful) | **Safety Guardrails** — warns before destructive commands. |
| [`/freeze`](/skills/freeze) | **Edit Lock** — restrict edits to one directory. |
| [`/guard`](/skills/guard) | **Full Safety** — /careful + /freeze combined. |
| [`/unfreeze`](/skills/unfreeze) | **Unlock** — remove the /freeze boundary. |
| [`/setup-deploy`](/skills/setup-deploy) | **Deploy Configurator** — one-time setup for /land-and-deploy. |
| [`/jstack-upgrade`](/skills/jstack-upgrade) | **Self-Updater** — upgrade jstack to latest version. |

## Parallel sprints

jstack works well with one sprint. It gets interesting with ten running at once.

**Design is at the heart.** `/design-consultation` researches what's out there in your space, proposes creative risks, generates realistic mockups, and writes `DESIGN.md` — then `/design-review` and `/plan-eng-review` read what you chose. Design decisions flow through the whole system.

**`/qa` was a massive unlock.** Claude Code saying "I SEE THE ISSUE" and then actually fixing it, generating a regression test, and verifying the fix — that's what 6-to-12 parallel workers looks like.

**Smart review routing.** CEO doesn't review infra bug fixes. Design review isn't needed for backend changes. The Review Readiness Dashboard tells you where you stand before you ship.

**Test everything.** `/ship` bootstraps test frameworks from scratch. Every `/ship` run produces a coverage audit. Every `/qa` bug fix generates a regression test.

**Real browser mode.** `$B connect` launches your actual Chrome as a headed window controlled by Playwright. You watch Claude click, fill, and navigate in real time. `$B disconnect` returns to headless.

With [Conductor](https://conductor.build), you can run 10-15 parallel sprints — each in its own isolated workspace. The sprint structure is what makes parallelism work. Without a process, ten agents is ten sources of chaos. With a process, each agent knows exactly what to do and when to stop.
