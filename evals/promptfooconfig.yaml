description: jstack skill and routing evals

prompts:
  - "{{prompt}}"

providers:
  - id: anthropic:messages:claude-sonnet-4-6
    config:
      max_tokens: 1024

defaultTest:
  options:
    provider:
      id: anthropic:messages:claude-sonnet-4-6

tests:
  # Per-plugin routing tests
  - file://cases/claude-ecosystem-routing.yaml
  - file://cases/gitlab-dev-routing.yaml
  - file://cases/golang-dev-routing.yaml
  - file://cases/nix-dev-routing.yaml
  - file://cases/obsidian-routing.yaml
  - file://cases/productivity-routing.yaml
  - file://cases/rust-dev-routing.yaml
  - file://cases/skill-creator-routing.yaml
  # Cross-plugin discovery tests
  - file://cases/discovery-tests.yaml
