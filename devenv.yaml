# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
imports:
  - ./modules/claude-skills.nix

inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/4c1018dae018162ec878d42fec712642d214fdfa
  treefmt-nix:
    url: github:numtide/treefmt-nix
    inputs:
      nixpkgs:
        follows: nixpkgs

secretspec:
  enable: true
  provider: env #onepassword  # keyring, dotenv, env, 1password, lastpass
  profile: development   # profile from secretspec.toml
# https://devenv.sh/binary-caching/
cachix:
  pull:
    - jylhis
    - devenv
